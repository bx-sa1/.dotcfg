#!/bin/bash

set -e

#pipe image to 0x0.st
[[ ! -p /dev/stdin ]] && (
  echo "No pipe"
  exit 1
)
link="$(curl -H "Content-Type: multipart/form-data" -F "file=@-" -X POST https://0x0.st)"
echo $link
notify-send "Screenshot uploaded to 0x0.st"
xdg-open $link
echo $link | xclip -selection clipboard
